diff --git a/CMakeLists.txt b/CMakeLists.txt
index 3a20028..0107487 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -651,17 +651,25 @@ if(ZLIB_FOUND)
   list(APPEND CMAKE_REQUIRED_INCLUDES ${ZLIB_INCLUDE_DIRS})
 endif()
 
-option(CURL_BROTLI "Set to ON to enable building curl with brotli support." OFF)
-set(HAVE_BROTLI OFF)
-if(CURL_BROTLI)
-  find_package(Brotli QUIET)
-  if(BROTLI_FOUND)
-    set(HAVE_BROTLI ON)
-    list(APPEND CURL_LIBS ${BROTLI_LIBRARIES})
-    include_directories(${BROTLI_INCLUDE_DIRS})
-    list(APPEND CMAKE_REQUIRED_INCLUDES ${BROTLI_INCLUDE_DIRS})
-  endif()
-endif()
+# option(CURL_BROTLI "Set to ON to enable building curl with brotli support." OFF)
+# set(HAVE_BROTLI OFF)
+# if(CURL_BROTLI)
+#   find_package(Brotli QUIET)
+#   if(BROTLI_FOUND)
+#     set(HAVE_BROTLI ON)
+#     list(APPEND CURL_LIBS ${BROTLI_LIBRARIES})
+#     include_directories(${BROTLI_INCLUDE_DIRS})
+#     list(APPEND CMAKE_REQUIRED_INCLUDES ${BROTLI_INCLUDE_DIRS})
+#   endif()
+# endif()
+# Force enable brotli
+set(BROTLI_LIBRARIES
+    brotlicommon
+    brotlidec
+    brotlienc
+)
+set(HAVE_BROTLI ON)
+list(APPEND CURL_LIBS ${BROTLI_LIBRARIES})
 
 option(CURL_ZSTD "Set to ON to enable building curl with zstd support." OFF)
 set(HAVE_ZSTD OFF)
@@ -1326,6 +1334,25 @@ if(USE_MANUAL)
   add_subdirectory(docs)
 endif()
 
+if(NOT BUILD_SHARED_LIBS)
+  set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
+  set(CMAKE_C_FLAGS_RELEASE "${CMAKE_C_FLAGS_RELEASE} /MT")
+  set(CMAKE_C_FLAGS_DEBUG "${CMAKE_C_FLAGS_DEBUG} /MTd")
+  add_definitions(-DNGHTTP2_STATICLIB)
+  set(CompilerFlags
+    CMAKE_CXX_FLAGS
+    CMAKE_CXX_FLAGS_DEBUG
+    CMAKE_CXX_FLAGS_RELEASE
+    CMAKE_C_FLAGS
+    CMAKE_C_FLAGS_DEBUG
+    CMAKE_C_FLAGS_RELEASE
+  )
+  foreach(CompilerFlag ${CompilerFlags})
+    string(REPLACE "/MD" "/MT" ${CompilerFlag} "${${CompilerFlag}}")
+  endforeach()
+endif()
+
+
 add_subdirectory(lib)
 
 if(BUILD_CURL_EXE)
diff --git a/src/curl.rc b/src/curl.rc
index 7d88b32..6e9296e 100644
--- a/src/curl.rc
+++ b/src/curl.rc
@@ -24,6 +24,8 @@
 
 LANGUAGE  0, 0
 
+ICONAPP ICON "curl.ico"
+
 #define RC_VERSION  CURL_VERSION_MAJOR, CURL_VERSION_MINOR, CURL_VERSION_PATCH, 0
 
 VS_VERSION_INFO VERSIONINFO
